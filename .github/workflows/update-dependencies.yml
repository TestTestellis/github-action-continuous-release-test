---
name: Update Dependencies

"on":
  schedule:
    - cron: 0/15 * * * *

jobs:
  update-dependencies:
    name: Update dependencies
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        # yamllint disable-line rule:line-length
        uses: actions/checkout@a12a3943b4bdde767164f792f33f40b04645d846 # tag=v3.0.0 editorconfig-checker-disable-line

      - name: Update dependencies
        # yamllint disable-line rule:line-length
        uses: renovatebot/github-action@a966d096bd20c290a6a63d9adf666709bb0f41be # tag=v32.17.1 editorconfig-checker-disable-line
        with:
          configurationFile: renovate.json5
          token: ${{ secrets.RENOVATE_GITHUB_TOKEN }}
